---
name: codex-quota-compass
description: 只读检查当前 Codex 订阅的周额度、已用和剩余比例、Credits、Token 分类、turns、threads，对比最近两个七日窗口，并用 Credits 除以周使用比例粗略反推总周额度。用户询问 Codex 20x 或 5x 额度、是否被降额、额度掉得快、还剩多少、想按 Linux.do 油猴脚本方法自查时使用。
---

# Codex Quota Compass

用本 Skill 做一次只读自查。它复现 Linux.do 油猴脚本的核心算法，同时把结果整理成适合普通用户阅读的报告。

## 执行

运行随附脚本。

```bash
python3 scripts/check_codex_quota.py
```

需要机器可读结果时运行。

```bash
python3 scripts/check_codex_quota.py --json
```

脚本会读取当前系统用户的 `~/.codex/auth.json`，只把访问令牌发送给 `https://chatgpt.com` 下三个只读接口。一个接口返回周窗口，一个接口返回每日用量，另一个接口返回按模型和 Token 类型拆分的明细。令牌不会打印、保存或写入报告。

部分账号的每日用量接口不再直接返回 `totals.credits`。遇到这种情况，脚本会尝试读取按模型拆分的非缓存输入、缓存输入和输出 Token，再按公开 Codex Rate Card 回算 Credits。这个备用接口依赖 ChatGPT 当前工作区，某些 Codex 登录态会拒绝返回数据。脚本会如实报告该情况，不会把缺失的 Credits 当成 0。

报告会同时显示 Credits 来源、计价覆盖率和无法计价的模型。计价不完整或备用接口不可用时，不反推周总额。这时告诉用户转到 ChatGPT Codex Analytics 页的 DevTools 运行浏览器脚本，因为浏览器登录态可能具有本地 Codex 凭证没有的工作区上下文。

## 报告顺序

先告诉用户当前账号能直接确认的内容。

- 套餐字段
- 周窗口已用和剩余比例
- 本次周窗口的重置时间
- 是否已经触顶

随后给出三组统计。

- 自动窗口按接口返回的周窗口起止时间汇总。后台只按自然日提供明细，重置发生在一天中间时，首日可能混入重置前的用量。
- 强制七天按最近七个自然日汇总。它方便和油猴脚本互证，不保证与真实重置周期完全重合。
- 两周对比把最近七个自然日与它之前的七天相比，同时列出 Credits、Token 和单位 Token 的 Credits 变化。这可以发现异常，但任务和模型不同时不能直接归因。
- Credits 来源为 `Rate Card` 时，覆盖率只表示当前模型和 Token 类型都能套用公开费率。按日汇总无法完整还原单次请求是否触发长上下文加价，接口没有速度字段时也无法确认 Fast 倍率。

最后给出反推值和判断边界。

- 反推总周额度等于自动窗口 Credits 除以周窗口已用比例。
- 周使用不足 20% 时明确写成低置信度估算。50% 左右通常更稳。
- 接口只返回 `pro`，不会直接返回 5x 或 20x。不要仅凭套餐字段判断档位。
- 原始 Token 不能直接换算额度。缓存输入、非缓存输入、输出、模型、推理强度和长上下文都会改变 Credits。
- 两周对比应尽量选择同模型、同速度、同类任务。如果工作负载改变，只报告现象，不下风控或降额的定论。
- 单次结果可以证明账号当前的计量表现，不能单独证明 OpenAI 对所有订阅统一降额。

## 安全边界

- 不输出 `auth.json` 内容、Bearer Token、邮箱、user_id 或 account_id。
- 不修改账号、指纹、登录态、额度或重置次数。
- 不调用 `chatgpt.com` 以外的主机。
- 不把完整接口响应发到公开论坛。
- 这三个后台接口没有稳定公开承诺。接口改动时，说明脚本需要更新，不要绕过认证或关闭安全检查。
